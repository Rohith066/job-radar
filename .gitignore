__pycache__/
*.py[cod]
*.egg-info/
.env
.venv/
venv/
config.yaml
# state/*.db is intentionally NOT ignored — the DB must persist between GitHub Actions runs.
# If you want to reset job history, delete state/jobs.db manually and recommit.
state/*.csv
